# Public frontend envs only (will be exposed to browser)
# Adjust ports/hosts to your local setup

NEXT_PUBLIC_JAVA_API_BASE=http://localhost:8081
NEXT_PUBLIC_GO_API_BASE=http://localhost:8080

# If you add non-public vars, prefer front/.env (server-only)
# and do NOT prefix with NEXT_PUBLIC_. For pure CSR apps, prefer only public vars.
